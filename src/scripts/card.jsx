import React from 'react'class Card extends React.Component {	getCardStatus() {		if ( this.props.accepts ) {			return "Accepted";		}		if ( this.props.rejects ) {			return "Rejected";		}	}	render() {		return (			<div { ...this.props } className="card">				<p>{ this.props.text }</p>				<p className="card-status">{ this.getCardStatus() }</p>			</div>		);	}}export default Card